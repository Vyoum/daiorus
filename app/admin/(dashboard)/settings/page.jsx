import AdminPlaceholder from '../_components/AdminPlaceholder';

export default function SettingsPage() {
  return (
    <AdminPlaceholder
      title="Settings"
      subtitle="Store settings UI will connect to SiteContent and env configuration."
    />
  );
}
