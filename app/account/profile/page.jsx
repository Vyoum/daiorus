import ProfileSettings from '../../../components/ProfileSettings';

export const metadata = {
  title: 'Profile Settings | DAIORUS',
};

export default function AccountProfilePage() {
  return <ProfileSettings />;
}
